# =========================================================
# 项目已迁移到 config.yaml 集中配置。
#
# 这里只保留一个环境变量：CONFIG_PATH
# 用于指定 config.yaml 的路径。
#
# 所有其他设置（密钥、配额、上传限制等）
# 请编辑项目根目录的 config.yaml。
# =========================================================

CONFIG_PATH=./config.yaml